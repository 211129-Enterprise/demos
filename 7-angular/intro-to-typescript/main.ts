function logIt(message) {
  console.log(message)
}

let myMessage = 'Hello World';

logIt(myMessage);

/**
 * 2 steps to execute the code:
 * 
 * 1. Call the typescript compiler to TRANSPILE our typescript to javascript -> tsc main.ts
 * 2. Run the JS file as node main.js
 */