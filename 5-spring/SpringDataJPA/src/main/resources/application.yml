spring:
  datasource:
    password: 867-5309a
    url: 'jdbc:postgresql://localhost:5432/postgres'
    username: 'postgres'
  jpa:
    hibernate:
      ddl-auto: create
      show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
