/**
 * = vs == and ===
 * 
 * = is an assignment operator
 * 
 * == is a comparator but performs something called type coercion
 * === also compares types
*/

console.log(2 == '2'); //true
console.log(2 === '2');//false