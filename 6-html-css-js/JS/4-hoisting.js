/**
 * Everytime the JS engine executes a script, an EXecution Context is build
 * 
 * 2 phases: creation, and execution
 */

/**
 * Creation Phase
 * 1. Create the global object
 * 2. Bind "this" to the global object
 * 3.Setup a memory heap
 * 4.Store the function declarations and variables with a global scope as undefined
 */